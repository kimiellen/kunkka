# kunkka

Kunkka is a local capability platform for multiple frontend forms, including browser extension UI, CLI frontend, TUI frontend, and future local UI surfaces.

The first implemented slice is the Rust workspace and `kunkka-ipc`, which defines the internal frame protocol, postcard serialization, and Unix Domain Socket transport.
